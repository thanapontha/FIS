# Properties file with JDBC and JPA settings.
#
# Applied by <context:property-placeholder location="jdbc.properties"/> from
# various application context XML files (e.g., "applicationContext-*.xml").
# Targeted at system administrators, to avoid touching the context XML files.

#-------------------------------------------------------------------------------
# Oracle Settings

jdbc.driverClassName=oracle.jdbc.OracleDriver
jdbc.url=jdbc:oracle:thin:@192.168.104.102:1521:KV0DEVT
jdbc.username=kv0user00
jdbc.password=kv0user00@fis

#BFW
bfw.jdbc.url=jdbc:oracle:thin:@192.168.104.102:1521:KV0DEVT
bfw.jdbc.username=kv0user00
bfw.jdbc.password=kv0user00@fis

# Property that determines which Hibernate dialect to use
# (only applied with "applicationContext-hibernate.xml")
hibernate.dialect=org.hibernate.dialect.Oracle10gDialect

# Property that determines which database to use with an AbstractJpaVendorAdapter
jpa.database=ORACLE
jpa.showSql=false